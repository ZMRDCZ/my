{
  "name": "dmitry-komarov-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Интерактивное портфолио Дмитрия Комарова",
  "homepage": "https://zmrdcz.github.io/my/",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && touch .output/public/.nojekyll && gh-pages -d .output/public",
    "deploy:manual": "NODE_ENV=production npm run generate && touch .output/public/.nojekyll && gh-pages -d .output/public"
  },
  "devDependencies": {
    "@nuxt/devtools": "^1.0.8",
    "@nuxtjs/google-fonts": "^3.0.2",
    "gh-pages": "^6.3.0",
    "nuxt": "^3.7.4"
  },
  "dependencies": {
    "@vueuse/core": "^10.5.0",
    "@vueuse/nuxt": "^10.5.0",
    "gsap": "^3.12.2",
    "sass": "^1.69.5",
    "vue-toastification": "^2.0.0-rc.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
